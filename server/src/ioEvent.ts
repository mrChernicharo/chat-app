const frontendEvents = {
	FRONTEND_SEND_MESSAGE: 'FRONTEND_SEND_MESSAGE',
};

const serverEvents = {
	SERVER_BROADCAST_MESSAGE: 'SERVER_BROADCAST_MESSAGE',
	CONNECTION: 'connection',
	DISCONNECT: 'disconnect',
};

export { frontendEvents, serverEvents };
